[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["npm ci || npm install"]

[phases.build]
cmds = ["rm -rf .next *.tsbuildinfo && npm run build"]

[start]
cmd = "npm start"
